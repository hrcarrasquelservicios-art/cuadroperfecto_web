{
  "scripts": {
    "generate:legacy": "node scripts/migrate-legacy.cjs",
    "syntax-check": "node --check js/main.js && node --check js/meeting-selection.js && node --check scripts/migrate-legacy.cjs && node --check scripts/snapshot-integrity.cjs && node --check scripts/seed-september-content.cjs && node --check scripts/freeze-current-snapshots.cjs && node --check tests/meeting-model.test.js && node --check js/jornada-model.js && node --check scripts/jornada-agent.cjs",
    "seed:september": "node scripts/seed-september-content.cjs",
    "freeze:current": "node scripts/freeze-current-snapshots.cjs",
    "build": "npm run generate:legacy && npm run freeze:current && npm run syntax-check && node scripts/build-pages.cjs",
    "lint": "eslint js tests",
    "test": "npm run generate:legacy && node tests/meeting-model.test.js && node tests/jornada.test.js",
    "agent:ingest": "node scripts/jornada-agent.cjs"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "sharp": "^0.35.2"
  }
}
